
module.exports = {
    index : (req, res) => {
        res.render("app/index")
    }
    // show: (req, res) => {
    //     Problem.findOne({_id: req.params.id}) 
    //     .then(console.log(req))       
    // }
}